import React from 'react';
import './Header.css';

export default function(props) {
  return render(
    <div>
      <nav className="navbar">
        <h1>{'{React.hobbies}'}</h1>
      </nav>
    </div>
  )
}

